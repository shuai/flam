<%= form_for(@video_resource) do |f| %>
  <% if @video_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@video_resource.errors.count, "error") %> prohibited this video_resource from being saved:</h2>

      <ul>
      <% @video_resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
